"""Querétaro Urban Lab — backend.

FastAPI application that wraps the `tesis_ac` package and exposes the urban
growth pipeline as a HTTP + WebSocket service to the desktop UI.
"""

__version__ = "0.1.0"
